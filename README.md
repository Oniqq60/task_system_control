# Система управления задачами для иерархических команд

## Быстрый старт

### Предварительные требования
- Docker и Docker Compose
- Go 1.23.4+
- Make (для миграций)

### Запуск проекта

1. **Запустите все сервисы:**
```bash
docker-compose up -d
```

2. **Создайте базу данных для Task Service:**
```bash
cd task
make create-db
```

Или вручную:
```bash
docker exec -i tasksystemcontrol-postgres-1 psql -U user -d taskdb -c "CREATE DATABASE task;"
```

3. **Примените миграции:**

Для Auth Service:
```bash
cd auth
make up
```

Для Task Service:
```bash
cd task
make up
```

## Как пользоваться 
1. Запускаем postman и импортируем 2 json файла (из директории api_gateway) с описанием эндпоинтов
2. Создаем 2 профиля admin и employee, админ умеет создавать и закрывать задачи (Complete)
3. Жмём Login - admin и после этого создаём задачу указав id работника  
4. Для привязки документа к выполненой задачи заходим в профиль работника, которого мы указали при создании задачи и вызываем Add document указав id задачи
5. Выбираем файл, задаём content_type (в случае pdf файла "application/pdf") и желательно задать имя файлу и теги во избежинии путаницы
   <img width="1360" height="1104" alt="Image" src="https://github.com/user-attachments/assets/357461d8-2e54-4c35-bbce-b91dce3a3a8c" />

6. ![Что бы не просто посмотреть файл, а так же его сохранить используем кнопку Send and Download](.\img\untitled (3).png) 
    
## Базы данных

Проект использует две отдельные базы данных PostgreSQL:
- **taskdb** - для Auth Service (таблица `users`)
- **task** - для Task Service (таблица `tasks`)

Обе БД находятся в одном контейнере PostgreSQL, но изолированы друг от друга.

## Структура проекта

```
.
├── auth/              # Auth Service (аутентификация)
├── document/          # Document Service (Управление документами)
├── task/              # Task Service (управление задачами)
├── notification/      # Notification Service (планируется)
├── api_gateway/       # API Gateway (планируется)
├── api/proto/         # Proto файлы для gRPC
└── gen/proto/         # Сгенерированный код из proto

```

## 📝 Примечания

- База данных `task` создаётся вручную после первого запуска контейнера PostgreSQL
- Миграции применяются отдельно для каждого сервиса
- Внешние ключи между таблицами разных БД не используются (микросервисная архитектура)

