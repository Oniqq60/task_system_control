package auth

const tokenBlacklistPrefix = "auth:token:blacklist:"

// только ради константы tokenBlacklistPrefix, потому что после удаления REST-хендлеров её единственное использование осталось в gRPC-обработчике (в grpc_handler.go). Раньше константа лежала в handler.go; когда этот файл мы удалили, gRPC код перестал компилироваться из‑за отсутствующего символа. Чтобы не дублировать строковый литерал в нескольких местах и не переписывать логику чёрного списка, вынес константу в отдельный файл, который подтягивает пакет auth. Так мы сохранили прежнее поведение (tokenBlacklistPrefix всё ещё общий для REST/gRPC) и избежали падения сборки. //
//Просто уже час ночи и как то в падлу сидеть и переделывать, по этому через костыли пошёл
